#include <iostream>
using namespace::std;
int mds(int nilai,int a){
    if
        (a==0)
        {return (nilai%a);}
    else if
        (a==nilai)
        {return (0);}
    else
        {return (nilai%a);}
    }


main(){
    int n1,n2;
    cout<<"input bilangan 1: ";cin>>n1;
    cout<<"input bilangan 2: ";cin>>n2;
    cout<<" Maka hasil = "<<mds(n1,n2);
    }
